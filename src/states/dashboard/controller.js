import template from './template.html'

export default {
    name: 'dashboard',
    abstract: true,
    template: template,
    controller: controller,
    controllerAs: 'ctrl'
}

/** @ngInject */
function controller() {
    const ctrl = this


}