import templateUrl from './template.html';

export default {
    name: 'dashboard',
    abstract: true,
    templateUrl: templateUrl,
    controller: controller,
    controllerAs: 'ctrl'
};

/** @ngInject */
function controller() {
    const ctrl = this;


}